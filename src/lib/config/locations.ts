/**
 * Sub-accounts are discovered from the agency app installation (see
 * src/lib/sync/discover.ts); nothing about them is hard-coded. A newly
 * discovered sub-account is tracked automatically if it has this pipeline.
 */
export const PIPELINE_NAME = "SMS Outreach - Remodelers - FB Ads";
